# Optional: API base URL (no trailing slash).
# Leave unset in dev to use Vite proxy ( /api -> http://localhost:8000 ).
# Set for production or custom backend, e.g. VITE_API_BASE=http://localhost:8000
# VITE_API_BASE=
