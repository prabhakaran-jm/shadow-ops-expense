# Call backend directly in dev to avoid Vite proxy issues with file uploads (e.g. receipt).
# Proxy can cause "write ECANCELED" on multipart/form-data. Backend must allow CORS from localhost:5173.
VITE_API_BASE=http://localhost:8000
